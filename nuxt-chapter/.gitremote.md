## 1 install github cli
- goto command prompt
```bash
    gh --version
    gh auth login
```
![alt text](1.jpg)

![alt text](2.jpg)
    - What account do you want to log into? `GitHub.com`
    - authorize with github user and password
## 2 create repository
```bash
    git init
    git add .
    git commit -m "initial first"
    git branch -M main
    git remote add origin https://github.com/limvichet/devops-test.git
    git branch -m master main
    git push -f -u origin main
```


## 3 Change Git identity globally
```bash
git config --global user.name "Vichet Lim"
git config --global user.email "limvichet917@gmail.com"
```